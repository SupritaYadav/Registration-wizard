# Components

- `StepOne.jsx` — personal information
- `StepTwo.jsx` — account details and password toggle
- `StepThree.jsx` — review and submit

The parent `App.jsx` owns the wizard step and the final `formData`.